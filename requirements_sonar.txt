bluerobotics-ping>=0.2.5
Pillow>=10.0
# Camera preview and single-ingest decoded-frame recording on Windows.
# Surveyor/Ping1D replay and the offline tests do not require camera input.
opencv-python>=4.8
