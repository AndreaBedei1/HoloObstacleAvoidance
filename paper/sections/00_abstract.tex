%==============================================================================
% ABSTRACT --- SKELETON ONLY.  Status: WAIT_SIM_REAL.
% Do NOT write the final abstract before the pool campaign and the S0-S3
% analysis exist.  The structure below fixes the argument; the bracketed
% quantities are filled from experiment manifests, never from expectation.
%==============================================================================
\begin{abstract}
% [1] Context: simulation is the default evaluation tool for underwater
%     obstacle avoidance because physical trials are scarce and costly.
% [2] Gap: prior work establishes that avoidance controllers CAN transfer;
%     it does not establish that simulation PREDICTS comparative physical
%     performance.
% [3] What we do: identical ROS 2 perception / temporal-estimation /
%     local-planning stack against HoloOcean and against a physical
%     BlueROV2 Heavy avoiding physical obstacles, with independent overhead
%     ground truth, under a progressive calibration ladder S0--S3 that
%     isolates camera, system-timing, and vehicle-response mismatch.
% [4] What we measure: absolute sim-vs-real error on safety and trajectory
%     metrics, and preservation of the RANKING of avoidance methods.
% [5] Headline numbers:
%     - sim-real gap per calibration level ................ \SREAL
%     - rank preservation (SRCC) per calibration level .... \SREAL
%     - number of physical trials ......................... \POOL
%     - planner comparison outcome ........................ \PEIGHT
% [6] Secondary result (already available, see Sec.~\ref{sec:results}):
%     a controlled ablation showing that simple temporal persistence
%     predicts obstacle motion through maneuver onset better than a
%     constant-velocity Kalman filter, while the Kalman filter earns its
%     place through gated outlier robustness --- i.e.\ prediction accuracy
%     and outlier robustness are separate benefits.
% [7] Takeaway sentence: to be written once [5] exists.
\PENDING{ABSTRACT --- structure fixed above; text is written only after the
S0--S3 ladder and the physical campaign produce numbers.}
\end{abstract}
