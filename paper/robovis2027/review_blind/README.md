# ROBOVIS 2027 double-blind review build

Submit only `main.pdf` from this directory. It is generated from
`../main.tex` by running `../build_review.ps1`; the same command also writes
`character_count.json` with the reproducible official-limit audit.
The counter requires Python with `pypdf` installed.

Do not add author metadata, affiliations, acknowledgements, repository links,
or the camera-ready AI disclosure to this build.
